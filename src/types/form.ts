export type FormValues = {
    peopleAmount: number;
    duration: number;
    isChildrenAllowed: boolean;
    // startDate: string | null;
    // endDate: string | null;
    // countries: string[];
    // tags: string[];
    // comments: string;
  };
